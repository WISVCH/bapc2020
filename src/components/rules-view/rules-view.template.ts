import { html } from 'lit-element';
import { BapcView } from './rules-view.component';

export default function template(this: BapcView) {
  return html`
    <section>
      <h1>Rules</h1>
    <p>The rules will be added here.</p>
    </section>
  `;
}
