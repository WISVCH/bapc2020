import { html } from 'lit-element';
import { BapcView } from './preliminaries-view.component';

export default function template(this: BapcView) {
  return html`
    <section>
      <h1>Preliminaries</h1>
      <p>More information on preliminaries will be added here later.</p>
    </section>
  `;
}
